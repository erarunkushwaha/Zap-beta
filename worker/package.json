{
  "name": "worker",
  "version": "1.0.0",
  "description": "this will take events from queue and do rest of the things",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src --ext ts --exec ts-node src/index.ts"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  }
}
